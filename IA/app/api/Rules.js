function Rules(){
	//se llama Rule porque rules es palabra reservada
	this.roundTime;
	this.turnTime;	
	this.roundNumber;
	this.BoardSize; //Ancho (Width) y Alto (Height)
}

module.exports.Rules = Rules;